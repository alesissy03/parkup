// TODO (Task 6):
// - initializează Leaflet.draw pe hartă
// - trimite poligoanele desenate către /api/admin/polygons
// TODO Task 6: inițializează harta cu Leaflet.draw
function initAdminMapWithDrawTools() {
  // creează harta, adaugă controlul Leaflet.draw
}

// TODO Task 6: trimite poligoanele desenate către backend
function sendPolygonsToBackend(geojson) {
  // POST /api/admin/polygons cu lots/spots
}

document.addEventListener("DOMContentLoaded", () => {
  console.log("admin_draw.js – entrypoint");
  // TODO Task 6: initAdminMapWithDrawTools();
});
